Notes:
- Splash alignment/black issue likely due to Splash vs SplashScreen mismatch; Login uses SplashScreen but user mentions starting animation effect is black. We'll update SplashScreen ring spinner (border-2 border-transparent + gradient animate-spin) to be visible and centered.
- TeacherDashboard alignment: currently uses plain `min-h-screen flex bg...` unlike others that use `glass-container min-h-screen` and consistent padding/z-index.
- Back redirects to login: likely because browser back returns to `/` which is login route protected? ProtectedRoute redirects to `/` if localStorage auth missing; ensure logout/back doesn't clear localStorage. But user says backing redirects directly to login (maybe by using back button from teacher routes where history includes login/splash). We'll add a proper back button handler inside TeacherDashboard/Upload to navigate back while keeping auth (e.g., navigate('/teacher') instead of window.history.back). Need check if any back button exists; none seen yet. Might be from browser back causing route change to '/' (login). Fix by using `ProtectedRoute` to redirect authenticated users away from '/' to their role dashboard.
teacher dashboard
admin dashboard